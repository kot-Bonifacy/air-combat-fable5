# MEMORY.md — indeks pamięci projektu

- [Faza 11 — decyzje](project_phase11_decisions.md) — walka sieciowa autorytatywna: rewind liczony z ECHA TICKU (`ackServerTick` w INPUT → `rewind = (tick−ack) + bufor`, pełny RTT, NIE „ping/2"; brak synchronizacji zegarów i wartości od klienta = nic do oszukania); cap rewindu 250 ms (NIE 200 ze spec. — ping 150 + bufor 100 to minimum przy modelu pełnego RTT); `PositionHistory` ring `tick%cap`, TYLKO pozycja (sfera izotropowa), bezpieczny na u32; `rewindTicks` = własność POCISKU, cofamy tylko CEL (nie strzelca/lecące pociski); pociski NIE w snapshotach — klient rysuje kosmetyczne smugacze z eventu MUZZLE (`createRng(seed)` = ten sam strumień co serwer); eventy walki BINARNE (`MSG_EVENT`, broadcast, klient filtruje po id), rozróżniane od snapshotu pierwszym bajtem; hit marker/feed/HP = echo serwera (zero hit-detekcji u klienta); protokół v2 (INPUT ack zamiast clientTimeMs, +1 B HP/encję); kredyt = `applyDamage`=='destroyed' (remis działa, asysty z `damagedBy` też przy śmierci od ziemi); benchmark 8 graczy = 0,476 ms/tick (dev); OTWARTE dla użytkownika po deployu: sesja 2-os. ping 150 ms + pomiar CPU na VPS
- [Faza 9 — decyzje](project_phase9_decisions.md) — multiplayer cz.2: wspólny `stepPilotedPlane` (shared/world/piloted-plane) używany przez serwer I predykcję klienta (niezmiennik reconciliation: replay = ten sam kod co serwer); ukryty stan maszyn NIE w snapshocie → tolerowany mikro-dryf, `iasMs` odtwarzane z prędkości; wygładzanie korekty = OFFSET RENDERU zanikający (τ=0,1 s), snap ≥ 50 m; interpolacja obcych: zegar po ticku serwera (nie Date.now), bufor 100 ms, ekstrapolacja ≤ 100 ms; symulator sieci (`rollDelayMs`) wpięty w NetClient TX/RX, panel tweakpane tylko DEV (tree-shaken z prod), overlay `[N]`; reset kamery przy teleporcie; pułapki: toroidalne metryki/lerp, orientacja spawnu vs aim w teście, noUncheckedIndexedAccess. BRAK `project_phase8_decisions.md` (decyzje fazy 8 tylko w auto-pamięci)
- [Faza 7 — decyzje](project_phase7_decisions.md) — deploy: statyczne demo `dogfight.tatanga.eu` (port 8087, wzorzec C). PUŁAPKI: `npm ci` w alpine zrywa na `Missing @emnapi/* from lock` (lock z Windows bez bindingów linux-musl) → `npm install` w Dockerfile; `net-status` łączył się zawsze z ws://localhost → zbramkowane do DEV (mixed-content na https); build context = korzeń repo (monorepo); `vite build` z katalogu klienta (root/cwd); jeden Cache-Control. KLIENT: ekran ładowania (PlaneModel.ready / timeout), webglcontextlost/restored, overlay „Jak grać" (localStorage) + atrybucja modelu. Zbudowane/przetestowane w Dockerze lokalnie; zostaje wdrożenie na VPS + tag demo-1
- [Faza 6 — decyzje](project_phase6_decisions.md) — bot AI: steruje WYŁĄCZNIE przez instruktora (dziedziczy kopertę); shared/ai = geometry/lead(kwadratowe, układ strzelca)/fsm(czyste przejścia)/difficulty(JSON+SI)/bot; limit G = clamp kąta komendy; degradacja G→reakcja→szum, ogień z prawdziwego leadu vs nos; unikanie ziemi = ciągły sufit AGL + lookahead terenu, maxDive 28° (roll rate spada do 14° przy IAS → roll-trap); mecz do 3 z respawnami; testy: 4boty/10min zero crashy + gunnery (łatwy<trudny)
- [Faza 5 — decyzje](project_phase5_decisions.md) — walka: opór pocisku kwadratowy + krok `p+=v·dt+½a·dt²` (semi-implicit zawyżał opad o ~1/n), konwergencja luf w (0,0,convM), rozrzut seeded RNG (2 liczby zawsze), pula prealokowana 768 + prevPosition anty-tunelowanie, smugacz co 3., HP celów w constants (nie JSON), TTK realistyczny hpPool=120/dmg=1.5
- [Faza 4 — decyzje](project_phase4_decisions.md) — siatka heightmapy 48 m (FP-dokładna, mesh↔kolizja `===`), simplex-noise z npm + mulberry32, lifecycle sygnalizuje a nie teleportuje, autopilot granicy = instruktor z celem na środek + histereza, clamp kamery do powierzchni
- [Faza 3 — decyzje](project_phase3_decisions.md) — instruktor: pull w G/rad + bramka bank-and-pull, sideslipMaxAccelG (zoom-climb artefakt), feed-forward koordynacji yaw, sustained turn: bank z liftDir.y i zakaz sprzężenia od vy, model Mk IA +12 lb ze źródłami, Tweakpane wymaga @tweakpane/core
- [Faza 2 — decyzje](project_phase2_decisions.md) — pitch→n bez regulatora wysokości, staticThrustN jako proxy spadku η śmigła, clMax gameplayowy 1.65, harness: punkt stały zamiast regulatorów P, pułapka 0/0→NaN w Cl
- [Faza 1 — decyzje](project_phase1_decisions.md) — mapowanie rate'ów na ω_body (−pitch,−yaw,+roll), tolerancje złotych testów vs błąd Eulera, scratch-wektory, F3 preventDefault
- [Faza 0 — decyzje](project_phase0_decisions.md) — shared bez builda (eksport źródeł .ts), esbuild dla serwera, TS przypięty ^5, strażnik warstw w ESLint
