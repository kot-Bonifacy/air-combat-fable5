# Faza 7 (demo statyczne) NIE potrzebuje żadnych zmiennych — frontend jest w 100%
# statyczny, bez backendu. Ten plik to szkielet pod fazę 13 (serwer WebSocket).
#
# Gdy dojdzie backend: skopiuj do `.env` NA VPS (nie wersjonuj — patrz .gitignore)
# i odkomentuj usługę `backend` w docker-compose.yml oraz blok /ws w nginx.conf.

# CLIENT_ORIGIN=https://dogfight.tatanga.eu
# NODE_ENV=production
